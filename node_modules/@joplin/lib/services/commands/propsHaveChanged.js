"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
function propsHaveChanged(previous, next) {
    if (!previous && next)
        return true;
    if (previous && !next)
        return true;
    if (!previous && !next)
        return false;
    if (Object.keys(previous).length !== Object.keys(next).length)
        return true;
    for (const n in previous) {
        if (previous[n] !== next[n]) {
            return true;
        }
    }
    return false;
}
exports.default = propsHaveChanged;
//# sourceMappingURL=propsHaveChanged.js.map